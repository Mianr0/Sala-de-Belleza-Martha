<section id="contacto" class="contact">
  <div class="container">
    <h2>Contáctanos</h2>
    <div class="contact-content">
      <div class="contact-info">
        <h3>Información de Contacto</h3>
        <div class="contact-item">
          <strong>📍 Dirección:</strong>
          <p>Calle 12 # 8-39, Barrio Centro de el Espinal - Tolima</p>
        </div>
        <div class="contact-item">
          <strong>📞 Teléfono:</strong>
          <p>+57 3114820079</p>
        </div>
        <div class="contact-item">
          <strong>⏰ Horarios:</strong>
          <p>
            Lunes a Viernes: 8:00 AM - 7:00 PM<br />
            Sábados: 8:30 AM - 6:00 PM<br />
            Domingos: 10:00 AM - 4:00 PM
          </p>
        </div>
      </div>
      <div class="contact-form">
        <h3>Agenda tu Cita</h3>
        <form>
          <input type="text" placeholder="Tu nombre" required />
          <input type="email" placeholder="Tu email" required />
          <input type="tel" placeholder="Tu teléfono" required />
          <select required>
            <option value="">Selecciona un servicio</option>
            <option value="cortes">Cortes</option>
            <option value="peinados">Peinados</option>
            <option value="cepillados">Cepillados</option>
            <option value="trenzas">Trenzas</option>
            <option value="tinturas">Tinturas</option>
            <option value="queratinas">Queratinas</option>
          </select>
          <textarea placeholder="Mensaje adicional (opcional)"></textarea>
          <button type="submit">Agendar Cita</button>
        </form>
      </div>
    </div>
  </div>
</section>

<style>
  .contact {
    margin: 10rem 0 4rem 0;
    padding: 0 1rem;
  }

  .container {
    max-width: 1100px;
    margin: 0 auto;
  }

  h2 {
    font-size: 3.2rem;
    font-weight: bold;
    font-family: "Allura";
    color: #ff8d8d;
    text-align: center;
    letter-spacing: 0.5px;
    margin-bottom: 2rem;
  }

  .contact-content {
    display: grid;
    grid-template-columns: 1.1fr 1fr;
    gap: 2rem;
    align-items: start;
  }

  .contact-info,
  .contact-form {
    background: #ffffff;
    border-radius: 16px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
    padding: 2rem;
    border: 1px solid rgba(0, 0, 0, 0.04);
  }

  .contact-info h3,
  .contact-form h3 {
    font-size: 1.6rem;
    color: #333;
    margin: 0 0 1rem 0;
  }

  .contact-item {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: 0.5rem 1rem;
    align-items: start;
    padding: 1rem 0;
    border-bottom: 1px dashed rgba(0, 0, 0, 0.08);
  }
  .contact-item:last-child {
    border-bottom: none;
  }

  strong {
    color: #444;
    font-weight: 700;
  }

  p {
    font-size: 1rem;
    line-height: 1.6;
    color: #666;
    margin: 0;
  }

  form {
    display: grid;
    gap: 1rem;
    margin-top: 0.5rem;
  }

  input,
  select,
  textarea {
    width: 100%;
    border: 1px solid #e8e8e8;
    background: #fafafa;
    padding: 0.85rem 1rem;
    border-radius: 12px;
    font-size: 0.95rem;
    color: #333;
    outline: none;
    transition:
      border-color 0.2s ease,
      box-shadow 0.2s ease,
      background 0.2s ease;
  }

  textarea {
    min-height: 120px;
    resize: vertical;
  }

  input:focus,
  select:focus,
  textarea:focus {
    border-color: #ffb3b3;
    box-shadow: 0 0 0 4px rgba(255, 141, 141, 0.15);
    background: #fff;
  }

  button[type="submit"] {
    background: linear-gradient(135deg, #ff8d8d, #ff6b6b);
    color: #fff;
    border: none;
    padding: 0.9rem 1.25rem;
    border-radius: 12px;
    font-weight: 700;
    letter-spacing: 0.3px;
    cursor: pointer;
    transition:
      transform 0.15s ease,
      box-shadow 0.2s ease,
      filter 0.2s ease;
  }

  button[type="submit"]:hover {
    transform: translateY(-1px);
    box-shadow: 0 10px 18px rgba(255, 107, 107, 0.25);
    filter: brightness(1.02);
  }

  button[type="submit"]:active {
    transform: translateY(0);
    box-shadow: 0 6px 12px rgba(255, 107, 107, 0.18);
  }

  @media (max-width: 1024px) {
    .contact-content {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 640px) {
    h2 {
      font-size: 2.6rem;
      margin-bottom: 1.5rem;
    }
    .contact-info,
    .contact-form {
      padding: 1.25rem;
      border-radius: 14px;
    }
    input,
    select,
    textarea {
      border-radius: 10px;
    }
  }
</style>
